import { vi, describe, it, expect, beforeEach } from 'vitest';

const sampleConfig = { connectionUrl: 'postgresql://user:pass@localhost:5432/db' };

describe('postgres driver', () => {
  beforeEach(() => {

  });

  it('tests connection', async () => {

  });

});

