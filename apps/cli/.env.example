# Query Agent Configuration
# These variables are used by FactoryAgent from @qwery/agent-factory-sdk

# Agent Provider (azure, ollama, vllm webllm)
VITE_AGENT_PROVIDER=vllm
# Or use AGENT_PROVIDER (both work)
AGENT_PROVIDER=VLLM

# VLLM Configuration
VLLM_BASE_URL=http://localhost:8000/v1
VLLM_MODEL=vllm/qwen2.5-coder-7b-instruct
VLLM_API_KEY=test-abc123

# Azure OpenAI Configuration
# AZURE_API_KEY=your-azure-api-key-here
# AZURE_RESOURCE_NAME=your-azure-resource-name
# AZURE_OPENAI_DEPLOYMENT=gpt-5-mini

# Optional: Azure API Version and Base URL
# AZURE_API_VERSION=
# AZURE_OPENAI_BASE_URL=

# Workspace Directory
# This is where FactoryAgent stores DuckDB files for Google Sheets
# Required for Google Sheets support via ReadDataAgent
VITE_WORKING_DIR=workspace
# Also set as WORKSPACE for compatibility
WORKING_DIR=workspace

# Optional: Ollama Configuration (if using ollama provider)
# OLLAMA_BASE_URL=http://localhost:11434
# OLLAMA_MODEL=llama2

# Optional: WebLLM Configuration (if using webllm provider)
# WEBLLM_MODEL=
# WEBLLM_TEMPERATURE=
